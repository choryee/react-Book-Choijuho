import React, { useEffect, useState } from 'react';
import BookItem from '../../../components/BookItem';

const Home = (props) => {
    const{author, title}=props;
    console.log('props====>>>', props); //HomePage.js에 넘긴것 넘어옴.

    return (
        <div>
         

        </div>
    );
};

export default Home;